#pragma once
#include "./socket_can.hpp"

namespace can
{
    class can_bus
    {
    private:
        /* data */
    public:
        can_bus(/* args */);
        ~can_bus();
    };
    
    can_bus::can_bus(/* args */)
    {
    }
    
    can_bus::~can_bus()
    {
    }
    
} // namespace can
